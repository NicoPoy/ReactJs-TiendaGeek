import NavBar from './NavBar.jsx'

// Header contiene la navegacion principal y queda visible arriba del sitio.
function Header() {
  return (
    <header className="site-header">
      <NavBar />
    </header>
  )
}

export default Header
